version: 2
jobs:
  build:
    docker:
      - image: circleci/php:7.2.0
    steps:
      - run:
          command: |
            echo "This is first step"
            exit 1
      - run:
          command: echo "This is second step"
          when: always